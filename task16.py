ball = int(input(f' Ball 1:'))
max_ball = ball

ball = int(input(f' Ball 2:'))
max_ball = ball
if ball > max_ball :
    max_ball = ball

ball = int(input(f' Ball 3:'))
max_ball = ball

max_ball = ball
if ball > max_ball :
    max_ball = ball
    
ball = int(input(f' Ball 4:'))
max_ball = ball

max_ball = ball
if ball > max_ball :
    max_ball = ball
    
ball = int(input(f' Ball 5:'))
max_ball = ball

max_ball = ball
if ball > max_ball :
    max_ball = ball
    
ball = int(input(f' Ball 6:'))
max_ball = ball

max_ball = ball
if ball > max_ball :
    max_ball = ball
    
ball = int(input(f' Ball 7:'))
max_ball = ball

max_ball = ball
if ball > max_ball :
    max_ball = ball
    
print(max_ball)

